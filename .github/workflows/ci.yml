name: CI

on:
  pull_request:
  push:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Use Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install
        run: npm ci

      - name: Lint
        run: npm run lint --if-present

      - name: Typecheck
        run: npm run check --if-present

      - name: Build
        run: npm run build

      # Minimal internal link checker (internal links only; avoids flaky externals)
      - name: Link check (internal)
        continue-on-error: true # warn-only until baseline is stable
        run: |
          npx -y http-server ./dist -p 4173 -c-1 &
          for i in 1 2 3 4 5; do
            curl -fsS http://localhost:4173/ >/dev/null && break
            sleep 1
          done
          npx -y linkinator http://localhost:4173 --recurse --skip '^(https?:)?//'

      # Optional: Lighthouse CI (usually better against preview URLs)
      # - name: Lighthouse CI
      #   run: |
      #     npm i -D @lhci/cli
      #     npx lhci autorun
