# Skills (repo-local)

These are **repo-specific workflows** for coding agents.

- `../skills.sh` is the single entrypoint for checks.

## Quick commands
- `./skills.sh check` → lint + format check + build
- `./skills.sh format` → prettier write

## Conventions
See:
- `../AGENTS.md`
- `../docs/rules/code-quality.md`
